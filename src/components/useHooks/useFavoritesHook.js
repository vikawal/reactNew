// import { useState } from 'react';

// export const useFavoritesHook = () => {
//   const [favorites, setFavorites] = useState([]);

//   const isFavorited = (cca3) => favorites.includes(cca3);

//   const toggleFavorite = (cca3) => {
//     if (isFavorited(cca3)) {
//       setFavorites(favorites.filter((c) => c !== cca3));
//     } else {
//       setFavorites([...favorites, cca3]);
//     }
//   };

//   return { favorites, toggleFavorite, isFavorited };
// };
